#import "@/components/UserImageWithPopup/userFragment.graphql"

query ($skip: Int!, $take: Int!, $sortBy: EventSortField, $order: SortOrder, $filter: String) {
  events(sortBy: $sortBy, order: $order, skip: $skip, take: $take, filter: $filter) {
    items {
      id
      title
      color
      date
      endDate
      creators {
        ...UserImageWithPopupUserFields
      }
    }
    hasMore
  }
}
