query ($skip: Int!, $take: Int!, $order: SortOrder, $sortBy: UploadedFileSortField) {
  uploadedFiles(order: $order, skip: $skip, take: $take, sortBy: $sortBy) {
    items {
      id
      title
      alias
      mimeType
      uploadDate
    }
    hasMore
  }
}
