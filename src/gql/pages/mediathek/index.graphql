query {
    videos(orderBy: [{ publicationDate: desc }]) {
        id
        title
        publicationDate
        youtubeVideoId
    }

    settingsSingletons {
        mediaLibraryPageContent {
            document(hydrateRelationships: true)
        }
    }
}
