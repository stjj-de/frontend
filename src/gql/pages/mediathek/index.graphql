query {
    videos(take: 15, orderBy: [{ publicationDate: desc }]) {
        id
        title
        publicationDate
        youtubeVideoId
    }

    settings {
        mediaLibraryPageContent
    }
}
