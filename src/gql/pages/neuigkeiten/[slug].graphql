query ($slug: String!) {
    post(where: { slug: $slug }) {
        title
        publicationDate
        authors(orderBy: [{ priority: asc }]) {
            id
            displayName
            role
            image {
                url
            }
        }
        content {
            document(hydrateRelationships: true)
        }
    }
}
